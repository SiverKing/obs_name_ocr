# OBS Name OCR 技术文档

## 索引目录

- [一、脚本代码分析解析](#一脚本代码分析解析)
  - [1. 项目整体定位](#1-项目整体定位)
  - [2. 文件职责划分](#2-文件职责划分)
  - [3. worker.py 主运行逻辑](#3-workerpy-主运行逻辑)
  - [4. 配置读取和热更新规则](#4-配置读取和热更新规则)
  - [5. 目标文本读取规则](#5-目标文本读取规则)
  - [6. 截图来源选择规则](#6-截图来源选择规则)
  - [7. OCR 识别和结果规范化规则](#7-ocr-识别和结果规范化规则)
  - [8. 命中匹配规则](#8-命中匹配规则)
  - [9. 框选消息生成规则](#9-框选消息生成规则)
  - [10. OBS 浏览器叠加层逻辑](#10-obs-浏览器叠加层逻辑)
  - [11. Windows 桌面透明叠加层逻辑](#11-windows-桌面透明叠加层逻辑)
  - [12. OBS Python 控制脚本逻辑](#12-obs-python-控制脚本逻辑)
  - [13. 辅助脚本逻辑](#13-辅助脚本逻辑)
- [二、用到的技术和技术点分析解析](#二用到的技术和技术点分析解析)
  - [1. Python 异步编程 asyncio](#1-python-异步编程-asyncio)
  - [2. HTTP 和 WebSocket 协议](#2-http-和-websocket-协议)
  - [3. OBS WebSocket 5.x 截图接口](#3-obs-websocket-5x-截图接口)
  - [4. RapidOCR 和 ONNX Runtime](#4-rapidocr-和-onnx-runtime)
  - [5. CUDA / DirectML 推理加速](#5-cuda--directml-推理加速)
  - [6. mss 屏幕截图](#6-mss-屏幕截图)
  - [7. NumPy 图像数组](#7-numpy-图像数组)
  - [8. OpenCV / Pillow 图像解码](#8-opencv--pillow-图像解码)
  - [9. Canvas 2D 绘制](#9-canvas-2d-绘制)
  - [10. Win32 API 桌面透明窗口](#10-win32-api-桌面透明窗口)
  - [11. ctypes 调用系统 API](#11-ctypes-调用系统-api)
  - [12. JSON 配置和深度合并](#12-json-配置和深度合并)
  - [13. 文本匹配和大小写处理](#13-文本匹配和大小写处理)
  - [14. 坐标缩放和区域映射](#14-坐标缩放和区域映射)
  - [15. 日志、容错和退出控制](#15-日志容错和退出控制)
  - [16. RapidOCR 原生 TensorRT 实测踩坑](#16-rapidocr-原生-tensorrt-实测踩坑)
- [三、GUI 控制台技术分析](#三gui-控制台技术分析)
  - [1. GUI 整体定位](#1-gui-整体定位)
  - [2. 界面结构和布局](#2-界面结构和布局)
  - [3. 配置表单双向绑定](#3-配置表单双向绑定)
  - [4. name.txt 文本编辑](#4-nametxt-文本编辑)
  - [5. worker 进程管理](#5-worker-进程管理)
  - [6. OBS 连接测试与捕获对象选择](#6-obs-连接测试与捕获对象选择)
  - [7. 日志查看机制](#7-日志查看机制)
  - [8. 线程模型和界面响应性](#8-线程模型和界面响应性)
  - [9. 样式和视觉设计](#9-样式和视觉设计)
  - [10. GUI 涉及的关键技术点](#10-gui-涉及的关键技术点)

---

# 一、脚本代码分析解析

## 1. 项目整体定位

这个项目是一个面向 OBS 和 Windows 桌面的 OCR 命中文本框选工具。

核心目标是：

1. 从屏幕区域或 OBS WebSocket 截图接口获取画面。
2. 使用 RapidOCR 识别画面中的文字。
3. 把 OCR 结果和 `name.txt` 里的目标文本做匹配。
4. 匹配成功后生成矩形框数据。
5. 通过 WebSocket 推送给 `overlay.html`，让 OBS 浏览器源画出框。
6. 可选地创建 Windows 桌面透明置顶窗口，直接在本机屏幕上画出框。

项目的主程序不是 OBS 插件本体，而是独立 Python worker。OBS 里的 `obs_name_ocr.py` 只负责启动和停止 worker，避免把 OCR 这种耗时任务放到 OBS 渲染线程里。

## 2. 文件职责划分

| 文件 | 作用 |
| --- | --- |
| `worker.py` | 核心程序。负责加载配置、截图、OCR、匹配、WebSocket 服务、OBS 截图请求、桌面透明叠加层和主循环。 |
| `gui.py` | 本地桌面控制台（PySide6）。用于编辑 `name.txt`、以表单方式编辑 `config.json`、启动/停止 worker、测试 OBS 连接和查看最近日志。 |
| `obs_name_ocr.py` | OBS Python 脚本。提供 OBS 脚本面板按钮，用于启动或停止 `worker.py`。 |
| `overlay.html` | OBS 浏览器源页面。连接 worker 的 `/ws`，接收框数据并用 Canvas 绘制。 |
| `config.json` | 运行配置。控制识别频率、截图来源、OCR 参数、匹配规则、画框样式和桌面叠加层。 |
| `name.txt` | 目标文本列表。每行一个目标，OCR 结果命中这些目标后才画框。 |
| `list_obs_sources.py` | 辅助脚本。连接 OBS WebSocket，列出场景、输入源和 UUID。 |
| `list_windows.py` | 辅助脚本。列出当前可见 Windows 窗口标题、窗口类名和进程名。 |
| `requirements.txt` | Python 依赖列表。 |
| `start.bat` | 启动脚本。激活虚拟环境后运行 `gui.py`。 |
| `worker.log` | worker 运行日志。记录启动、性能、异常等信息。 |
| `gui_worker.log` | GUI 启动 worker 时捕获的子进程标准输出和错误。 |

## 3. worker.py 主运行逻辑

`worker.py` 是整个项目的核心入口，执行顺序可以理解为下面这条链路：

```text
main()
  -> asyncio.run(main_async())
    -> 初始化日志
    -> 读取 config.json
    -> 启动 HTTP/WebSocket 服务
    -> 创建 recognition_loop 识别任务
    -> 等待退出信号
    -> 关闭识别任务、WebSocket 服务、桌面透明层和 OBS WebSocket 连接
```

`main_async()` 做的事情比较清晰：

1. 调用 `setup_logging()`，让日志同时输出到终端和 `worker.log`。
2. 调用 `load_config()` 读取配置，如果配置文件不存在，会按默认配置创建。
3. 从配置中读取 `host` 和 `port`。
4. 创建 `OverlayServer`，启动 HTTP/WebSocket 服务。
5. 创建 `recognition_loop()` 异步任务。
6. 安装 `SIGINT`、`SIGTERM` 退出信号处理。
7. 程序退出时主动清理资源。

`recognition_loop()` 是识别主循环，主要流程是：

```text
循环运行，直到 stop_event 被设置
  -> 按 reload_files_interval_ms 定期重读 config.json 和 name.txt
  -> 根据 capture.source 获取一帧图像
  -> 如果目标列表不为空，就执行 OCR
  -> 过滤低置信度结果
  -> 用 OCR 文本匹配 name.txt 目标文本
  -> 命中后生成 box 数据
  -> 生成 message
  -> 推送给 OBS 浏览器源
  -> 推送给桌面透明叠加层
  -> 按 interval_ms 控制下一轮识别时间
```

这种设计让截图、OCR、推送和绘制分层处理。识别循环只负责生产结果，浏览器源和桌面叠加层只负责消费结果并绘制。

## 4. 配置读取和热更新规则

配置文件是 `config.json`。程序里有一份 `DEFAULT_CONFIG` 默认配置，读取真实配置时会使用 `deep_merge()` 做深度合并。

深度合并规则是：

1. 默认配置作为基础。
2. `config.json` 中存在的字段覆盖默认值。
3. 如果某个字段本身是字典，则递归合并。
4. 如果某个字段不是字典，则直接覆盖。

这样做的好处是：配置文件可以只写自己关心的字段，缺失字段仍然有默认值，不容易因为少写一个嵌套字段导致程序报错。

配置会在运行中热更新。`recognition_loop()` 会按 `ocr.reload_files_interval_ms` 定期重读：

```json
"ocr": {
  "backend": "onnxruntime",
  "reload_files_interval_ms": 2000
}
```

也就是说，修改 `config.json` 或 `name.txt` 后通常不需要重启 worker，等待下一次重载即可生效。`backend`、TensorRT 精度或相关引擎参数变化时，`RapidOCREngine` 会比较新的参数字典与当前参数，并重新初始化或加载对应引擎。

热重载只覆盖配置和目标文本，不会重新加载 Python 模块。修改 `worker.py`、`gui.py` 或运行时兼容逻辑后必须重启 worker。

## 5. 目标文本读取规则

目标文本文件是 `name.txt`，由 `read_targets_and_groups()` 读取，`parse_targets()` 负责解析目标和分组。

读取规则是：

1. 使用 UTF-8 编码读取。
2. 每行一个目标文本。
3. 自动去掉行首尾空白。
4. 空行会被忽略。
5. 以 `#` 开头的行会更新当前分组；后续目标归入它上方最近的分组。
6. 命中框标签在有分组时显示为 `分组-目标`，没有分组时只显示目标名。
7. 读取失败时返回空列表和空分组映射，并记录日志。

只有当 `targets` 不为空时，主循环才会执行 OCR 匹配。目标列表为空时，程序仍然会运行并推送空框数据。

## 6. 截图来源选择规则

截图由 `capture_frame()` 统一处理。它根据 `config.json` 里的 `capture.source` 决定使用哪种截图方式。

### screen 模式

当 `capture.source` 是 `screen` 或未配置时，程序使用 `mss` 截取 Windows 屏幕区域。

相关配置示例：

```json
"capture": {
  "source": "screen",
  "monitor": 1,
  "left": 0,
  "top": 0,
  "width": 1920,
  "height": 1080
}
```

`resolve_capture_region()` 会先从 `mss.monitors` 中找到指定显示器，再把 `left`、`top` 当作显示器内部偏移，最终得到实际屏幕坐标。

最终截图区域是：

```text
实际 left = 显示器 left + 配置 left
实际 top  = 显示器 top + 配置 top
width     = 配置 width
height    = 配置 height
```

### obs_websocket 模式

当 `capture.source` 是 `obs_websocket`、`obs` 或 `obs-websocket` 时，程序使用 `OBSWebSocketScreenshotClient` 调用 OBS 的 `GetSourceScreenshot` 接口。

相关配置示例：

```json
"capture": {
  "source": "obs_websocket",
  "obs_websocket": {
    "url": "ws://127.0.0.1:4455",
    "password": "",
    "source_name": "窗口采集",
    "source_uuid": "",
    "image_format": "png",
    "image_width": 0,
    "image_height": 0,
    "image_compression_quality": 80
  }
}
```

规则是：

1. 优先使用 `source_uuid` 指定 OBS 源。
2. 如果没有 UUID，则使用 `source_name`。
3. 如果两者都没有配置，则自动读取当前节目场景名称。
4. 调用 `GetSourceScreenshot` 获取 Base64 图片。
5. 优先用 OpenCV 解码图片，失败时回退到 Pillow。
6. 解码后得到 NumPy 图像数组，交给 OCR 使用。

## 7. OCR 识别和结果规范化规则

OCR 由 `RapidOCREngine` 类负责。

它的职责有四层：

1. 规范化 `ocr.backend`，缺失或非法值安全回退到 `onnxruntime`。
2. 根据后端生成 RapidOCR 参数，并在参数变化时热重建引擎。
3. 记录实际 ONNX Runtime provider，或明确记录 RapidOCR 原生 TensorRT 的精度与组件后端。
4. 把不同版本 RapidOCR 可能返回的结果结构统一转成 `OCRItem`。

后端映射如下：

| `ocr.backend` | RapidOCR 参数 | CUDA / DirectML 参数 |
| --- | --- | --- |
| `onnxruntime` | 保持 Det、Cls、Rec 默认 ONNX Runtime | 传递 `EngineConfig.onnxruntime.use_cuda/use_dml` |
| `tensorrt_fp32` | Det、Rec 使用 `EngineType.TENSORRT`，`use_fp16=false`、`use_int8=false` | 不传递 |
| `tensorrt_fp16` | Det、Rec 使用 `EngineType.TENSORRT`，`use_fp16=true`、`use_int8=false` | 不传递 |

当 `use_cls=true` 时，TensorRT 模式还会设置 `Cls.engine_type=EngineType.TENSORRT`；当 `use_cls=false` 时不设置 Cls，未使用的分类器保持 RapidOCR 默认 ONNX Runtime。

这里必须传 `rapidocr.EngineType` 枚举，不能直接传字符串 `"tensorrt"`。RapidOCR 3.8.4 的 `ParseParams.update_batch()` 会检查枚举参数，字符串会抛出 `TypeError`。

`_ensure_engine()` 同时保存 `_engine_params` 和 `_engine_backend`。如果新配置与当前完全一致，就复用现有实例；如果精度、后端或相关参数变化，就创建新实例。新实例初始化成功后才替换旧实例；失败时：

- 已有旧引擎：恢复旧引擎并继续识别，日志记录请求后端、失败原因和当前实际后端。
- 首次启动没有旧引擎：保持 worker 服务运行并发送空框，绝不伪装成 TensorRT 已启用。

`OCRItem` 是一个数据类，包含：

```text
text: OCR 识别出来的文字
confidence: 置信度
rect: 文本框矩形区域，包含 x、y、w、h
```

RapidOCR 不同版本可能返回对象、字典、列表或元组，所以代码里有一组规范化方法：

| 方法 | 作用 |
| --- | --- |
| `_unwrap()` | 从返回值中取出真正的结果主体。 |
| `_from_attributes()` | 兼容对象属性形式，例如 `boxes`、`texts`、`scores`。 |
| `_from_dict_container()` | 兼容字典形式，例如 `results`、`data`、`items`。 |
| `_entry_to_item()` | 把单条 OCR 结果转成 `OCRItem`。 |
| `rect_from_points()` | 把多边形点位或矩形坐标转成统一矩形。 |

这个规范化层很重要，因为它降低了 RapidOCR 版本变化带来的风险。后续匹配逻辑只需要处理统一的 `OCRItem`，不用关心原始 OCR 返回格式。

## 8. 命中匹配规则

匹配由 `find_match()` 完成，主要受 `config.json` 里的 `match` 控制。

配置示例：

```json
"match": {
  "mode": "contains",
  "case_sensitive": false,
  "min_confidence": 0.5
}
```

规则如下：

1. `min_confidence` 用来过滤低置信度 OCR 结果。
2. `case_sensitive` 为 `false` 时，会使用 `casefold()` 做大小写统一。
3. `mode` 为 `exact` 时，OCR 文本必须和目标文本完全一致。
4. `mode` 为 `contains` 时，只要 OCR 文本包含目标文本就算命中。
5. 如果多个目标都可能命中，代码会按 `name.txt` 中的顺序返回第一个命中的目标。

当前配置使用的是 `contains`，适合 OCR 文本可能带有前后字符的场景。

## 9. 框选消息生成规则

命中后，程序会生成一组 `boxes`。每个框大致包含：

```json
{
  "text": "OCR 原始文本",
  "matched": "命中的目标文本",
  "label": "分组-命中的目标文本",
  "confidence": 0.9876,
  "color": "#ff3b30",
  "x": 100,
  "y": 200,
  "w": 80,
  "h": 24
}
```

颜色由 `get_box_color()` 决定。

颜色规则是：

1. `overlay.color_mode` 为 `single` 时，全部使用 `overlay.stroke_color`。
2. `overlay.color_mode` 为 `by_target` 时，不同目标文本使用不同颜色。
3. `by_target` 模式下，代码会对目标文本做 SHA1 哈希，再映射到 `color_palette` 中的某个颜色。
4. 同一个目标文本每次得到的颜色是稳定的。

最终通过 `build_message()` 生成推送消息：

```json
{
  "type": "boxes",
  "timestamp": 1710000000000,
  "width": 1920,
  "height": 1080,
  "boxes": [],
  "overlay": {}
}
```

其中 `width` 和 `height` 是 OCR 输入图像的尺寸。浏览器源和桌面透明层会根据自己的显示尺寸做缩放。

## 10. OBS 浏览器叠加层逻辑

OBS 浏览器源使用 `overlay.html`。

它的运行逻辑是：

1. 页面加载后创建全屏透明 Canvas。
2. 连接 `ws://127.0.0.1:8765/ws`。
3. 收到 WebSocket 消息后解析 JSON。
4. 根据消息里的 `width`、`height` 和当前 Canvas 尺寸计算缩放比例。
5. 遍历 `boxes`，绘制矩形框。
6. 如果 `show_label` 为 true，就优先使用 `label` 在框上方绘制带分组的标签。
7. WebSocket 断开后清空画面，并在 1 秒后自动重连。

Canvas 缩放规则是：

```text
scaleX = canvas.width / message.width
scaleY = canvas.height / message.height
```

所以 OBS 浏览器源的宽高最好和截图源宽高保持一致，否则画框会被按比例拉伸。只要比例一致，坐标仍然可以正确对应。

`OverlayServer` 没有使用 Flask、FastAPI 等框架，而是基于 `asyncio.start_server()` 手写了一个轻量 HTTP/WebSocket 服务。

它支持的路径包括：

| 路径 | 作用 |
| --- | --- |
| `/` | 重定向到 `/overlay.html`。 |
| `/overlay.html` | 返回浏览器叠加层页面。 |
| `/ws` | WebSocket 通道，向浏览器源推送框数据。 |
| `/health` | 返回健康状态和当前连接数。 |
| `/favicon.ico` | 返回空响应，避免浏览器反复报 favicon。 |

## 11. Windows 桌面透明叠加层逻辑

桌面透明叠加层由 `DesktopOverlay` 类负责，是可选功能。

是否启用由配置控制：

```json
"desktop_overlay": {
  "enabled": true,
  "click_through": true,
  "hide_when_empty": true,
  "coordinate_mode": "screen_region",
  "screen_region": "auto"
}
```

它的工作方式是：

1. 启动一个独立后台线程。
2. 使用 Win32 API 创建一个无边框、置顶、透明、工具窗口。
3. 通过颜色键 `transparent_color` 把背景色变成透明。
4. 收到最新框数据后触发窗口重绘。
5. 使用 GDI 画矩形框和标签。
6. 如果没有命中框且 `hide_when_empty` 为 true，则隐藏窗口。

桌面透明层有两种坐标模式。

### capture 模式

`coordinate_mode` 为 `capture` 时，透明层直接使用截图区域的坐标。

适合场景：

1. 直接用 `screen` 模式截屏。
2. OBS 截图内容和实际屏幕区域完全一致。

### screen_region 模式

`coordinate_mode` 为 `screen_region` 时，程序会把 OCR 图片坐标按比例映射到指定屏幕区域。

适合场景：

1. OCR 来源是 OBS 的窗口采集。
2. OBS 返回的截图坐标不等于真实 Windows 屏幕坐标。
3. 需要把 OCR 结果映射回某个实际窗口位置。

当 `screen_region` 设置为 `"auto"` 时，程序会尝试读取 OBS 输入源设置里的 `window` 字段，再通过 Win32 枚举窗口，找到对应窗口的真实屏幕矩形。

如果自动定位失败，程序会退回手动配置的 `left`、`top`、`width`、`height`。

## 12. OBS Python 控制脚本逻辑

`obs_name_ocr.py` 是给 OBS 加载的 Python 脚本。它不做 OCR，只负责控制 worker。

主要逻辑是：

1. 在 OBS 脚本属性面板中提供识别频率、worker 路径、Python 路径。
2. 提供“启动 worker”和“停止 worker”按钮。
3. 启动前检查 `worker.py` 和虚拟环境 `python.exe` 是否存在。
4. 启动前把 OBS 面板里的 `interval_ms` 写入 `config.json`。
5. 使用 `subprocess.Popen()` 启动 worker。
6. worker 输出会写入 `obs_worker.log`。
7. OBS 卸载脚本时自动停止 worker。

这种设计把 OBS 脚本作为控制入口，把重任务交给外部进程，能减少 OBS 卡顿或崩溃风险。

## 13. 辅助脚本逻辑

### list_obs_sources.py

这个脚本用于排查和配置 OBS WebSocket 来源。

运行后会：

1. 连接 OBS WebSocket。
2. 如有密码则按 OBS WebSocket 认证流程生成认证串。
3. 请求 `GetSceneList` 获取场景列表。
4. 请求 `GetInputList` 获取输入源列表。
5. 打印场景名、输入源名、输入源类型和 UUID。

它主要用于确认 `config.json` 中的 `source_name` 或 `source_uuid` 应该填什么。

### list_windows.py

这个脚本用于排查 Windows 窗口匹配。

运行后会：

1. 枚举当前可见窗口。
2. 读取窗口标题。
3. 读取窗口类名。
4. 读取窗口所属进程名。
5. 按 `标题 | 类名 | 进程名` 的格式打印。

它主要服务于 `desktop_overlay.screen_region = "auto"` 的窗口定位问题。

---

# 二、用到的技术和技术点分析解析

## 1. Python 异步编程 asyncio

项目使用 `asyncio` 管理 HTTP/WebSocket 服务、OBS WebSocket 请求和主识别循环。

异步编程的好处是：程序可以在等待网络、等待浏览器连接、等待 OBS 返回截图时，不阻塞整个 worker。这里没有为每个连接创建大量系统线程，而是在同一个事件循环里调度多个任务。

核心使用点包括：

1. `asyncio.run()` 启动主事件循环。
2. `asyncio.start_server()` 启动本地 HTTP/WebSocket 服务。
3. `asyncio.create_task()` 创建识别循环任务。
4. `asyncio.wait_for()` 控制等待超时。
5. `asyncio.Event` 管理退出信号。

## 2. HTTP 和 WebSocket 协议

项目自己实现了一个轻量 HTTP/WebSocket 服务。

HTTP 用来给 OBS 浏览器源提供 `overlay.html` 页面，WebSocket 用来持续推送识别框数据。

WebSocket 的关键点是：

1. 浏览器先发起 HTTP Upgrade 请求。
2. 服务端根据 `Sec-WebSocket-Key` 计算 `Sec-WebSocket-Accept`。
3. 握手成功后，连接升级为 WebSocket。
4. 服务端把框数据封装成 WebSocket text frame 推送给浏览器。
5. 浏览器断开后会自动重连。

这里没有使用第三方 WebSocket 服务框架，而是直接处理 WebSocket 帧。代码更轻，但也要求对协议细节有基本掌握。

## 3. OBS WebSocket 5.x 截图接口

OBS WebSocket 是 OBS 提供的远程控制协议。项目主要使用它的 `GetSourceScreenshot` 请求。

相关技术点包括：

1. 使用 `obswebsocket.json` 子协议连接 OBS。
2. 接收 OBS 发送的 Hello 消息。
3. 如果 OBS 开启密码，则使用 salt 和 challenge 生成认证值。
4. 发送 Identify 消息完成认证。
5. 发送 Request 消息请求截图。
6. 接收 RequestResponse，解析 Base64 图片。

这个方案不需要写 C++ OBS 插件，适合低频截图 OCR，例如每 500ms 或 1000ms 识别一次。

## 4. RapidOCR 和 ONNX Runtime

RapidOCR 是 OCR 流程库，负责检测、方向分类、识别和结果组织。ONNX Runtime 是它支持的推理运行时之一，但不是唯一后端；本项目现在还可以直接使用 RapidOCR 3.8.4 自带的 `TRTInferSession`。

项目使用 RapidOCR 完成：

1. 文本检测：找出图片里哪里有文字。
2. 文本识别：把文字图片识别成字符串。
3. 置信度输出：给每条识别结果一个可信程度。
4. 文本框输出：返回文字所在区域。

项目额外做了一层结果规范化，因为不同版本 RapidOCR 的返回结构可能不同。规范化后，后续逻辑只处理统一的 `OCRItem`。

项目没有实现 ONNX Runtime TensorRT Execution Provider。两条 NVIDIA 路径的调用层级不同：

```text
ONNX Runtime CUDA:
worker -> RapidOCR -> OrtInferSession -> CUDAExecutionProvider

RapidOCR 原生 TensorRT:
worker -> RapidOCR -> TRTInferSession -> TensorRT Engine -> CUDA Runtime
```

因此日志判定也不同：

- ONNX Runtime 模式读取 session 的 `get_providers()`，记录 Det、Cls、Rec 的实际 provider。
- TensorRT session 没有 ONNX Runtime 的 `get_providers()`。worker 不调用它，而是记录“RapidOCR 原生 TensorRT”、FP32/FP16 以及 Det、Rec、可选 Cls 的实际后端。

## 5. CUDA / DirectML 推理加速

`config.json` 以单一 `backend` 字段选择推理路线：

```json
"ocr": {
  "backend": "onnxruntime",
  "use_cuda": true,
  "use_dml": false
}
```

合法后端只有：

| 值 | 含义 |
| --- | --- |
| `onnxruntime` | 关闭 TensorRT，使用原有 ONNX Runtime。 |
| `tensorrt_fp32` | RapidOCR 原生 TensorRT FP32。 |
| `tensorrt_fp16` | RapidOCR 原生 TensorRT FP16。 |

旧配置缺少 `backend` 时由 `DEFAULT_CONFIG` 补成 `onnxruntime`。非法值也会回退到 ONNX Runtime，并通过带缓存的警告函数避免每轮重复刷相同警告。

### ONNX Runtime CUDA / DirectML 路径

CUDA 是 NVIDIA 显卡常用的计算加速方案，DirectML 是 Windows 上的通用机器学习加速接口。

项目中的处理规则是：

1. `use_cuda` 为 true 时，尝试让 ONNX Runtime 使用 CUDA provider。
2. `use_dml` 为 true 时，尝试使用 DirectML provider。
3. 如果显卡 provider 不可用，ONNX Runtime 通常会回退到 CPU provider。
4. worker 会尝试把 `nvidia/*/bin` 目录加入 DLL 搜索路径，减少 CUDA/cuDNN DLL 找不到的问题。
5. 初始化后会记录 RapidOCR 实际使用的 provider，方便判断是否真的走了显卡。

加速是否生效不只取决于配置，还取决于安装的 `onnxruntime` 版本和本机 CUDA/cuDNN 环境。

### RapidOCR 原生 TensorRT 路径

TensorRT 模式的核心参数为：

```python
{
    "Det.engine_type": EngineType.TENSORRT,
    "Rec.engine_type": EngineType.TENSORRT,
    "EngineConfig.tensorrt.use_fp16": False,  # FP16 模式为 True
    "EngineConfig.tensorrt.use_int8": False,
}
```

`use_cls=true` 时再增加：

```python
"Cls.engine_type": EngineType.TENSORRT
```

这一模式不传 `EngineConfig.onnxruntime.use_cuda` 或 `use_dml`，避免把两套后端配置混在一起。初始化前 worker 会：

1. 把现有 `nvidia/*/bin` 加入当前进程 DLL 搜索路径。
2. 分别检查 `import tensorrt` 和 `from cuda.bindings import runtime as cudart`。
3. 对 RapidOCR 3.8.4 应用 `model_root_dir` 兼容初始化。
4. 调用 `RapidOCR(params=engine_params)`，由 RapidOCR 原生 TensorRT builder 构建或加载 Engine。

RapidOCR 默认设置保持不变：

| 参数 | 默认值 |
| --- | --- |
| workspace | 1GB |
| Det profile | min `1x3x32x32`，opt `1x3x736x736`，max `1x3x2048x2048` |
| Rec profile | min `1x3x48x32`，opt `6x3x48x320`，max `6x3x48x2048` |
| INT8 | 关闭 |
| cache_dir | RapidOCR 默认目录 |

当前约 1984x1120 的检测输入没有超过 Det 最大 profile，因此项目不覆盖 shape、workspace 或缓存目录。

默认 Engine 文件名包含模型名、GPU 架构和精度，例如 `ch_PP-OCRv4_det_mobile_sm75_fp32.engine`。FP32 和 FP16 自然映射到不同缓存文件，所以精度切换会加载或构建对应 Engine。

2026-08-05 已完成实机验证：

| 组件 | 版本 / 设备 |
| --- | --- |
| Python | 3.12.9 |
| RapidOCR | 3.8.4 |
| TensorRT cu12 | 10.16.1.11 |
| cuda-python / cuda-bindings | 12.9.7 |
| CUDA Runtime | 12.9 |
| GPU | RTX 2070 8GB，Compute Capability 7.5 / sm75 |

Det、Rec 的 FP32 和 FP16 Engine 均已成功构建并运行。

## 6. mss 屏幕截图

`mss` 是一个跨平台屏幕截图库。项目在 `screen` 模式下使用它截取屏幕区域。

它的特点是：

1. 截图速度较快。
2. 可以读取多个显示器信息。
3. 可以按 `left`、`top`、`width`、`height` 截取固定区域。
4. 返回结果可以直接转换成 NumPy 数组。

在本项目中，`mss` 主要负责把屏幕画面变成 OCR 可以处理的图像数组。

## 7. NumPy 图像数组

NumPy 用于表示和处理图像数据。

屏幕截图或 OBS 截图最终都会变成类似下面的数组：

```text
height x width x channel
```

OCR 引擎需要的是图像数据，而 NumPy 数组是 Python 图像处理领域非常常见的交换格式。

项目中 NumPy 的主要作用是：

1. 保存截图像素。
2. 从 OpenCV 解码结果接收图像。
3. 计算 OCR 返回点位的最小外接矩形。
4. 把多边形文本框统一成 `x`、`y`、`w`、`h`。

## 8. OpenCV / Pillow 图像解码

OBS WebSocket 返回的是 Base64 图片字符串，不是直接可用的图像数组。

项目解码流程是：

1. 去掉 Base64 头部，例如 `data:image/png;base64,`。
2. 做 Base64 解码得到二进制图片数据。
3. 优先用 OpenCV 的 `cv2.imdecode()` 解码。
4. 如果 OpenCV 解码失败，则用 Pillow 打开图片。
5. 最后统一转成 NumPy 图像数组。

OpenCV 解码速度通常较好，Pillow 作为回退方案可以提高兼容性。

## 9. Canvas 2D 绘制

`overlay.html` 使用浏览器 Canvas 2D API 绘制框。

用到的主要能力包括：

1. `clearRect()` 清空上一帧。
2. `strokeRect()` 绘制矩形框。
3. `fillRect()` 绘制标签背景。
4. `fillText()` 绘制命中文本。
5. `measureText()` 计算标签宽度。
6. 根据 `devicePixelRatio` 适配高 DPI 屏幕。

Canvas 的好处是轻量、透明、适合 OBS 浏览器源，并且绘制框这种需求不需要复杂 DOM。

## 10. Win32 API 桌面透明窗口

桌面透明叠加层使用 Windows 原生 Win32 API 实现。

关键窗口样式包括：

1. `WS_POPUP`：无边框弹出窗口。
2. `WS_EX_LAYERED`：分层窗口，支持透明效果。
3. `WS_EX_TRANSPARENT`：鼠标事件穿透。
4. `WS_EX_TOPMOST`：置顶显示。
5. `WS_EX_TOOLWINDOW`：工具窗口，不像普通应用窗口那样显示。
6. `WS_EX_NOACTIVATE`：显示时不抢焦点。

透明效果通过 `SetLayeredWindowAttributes()` 和颜色键实现。背景填充成指定透明色后，系统会把这个颜色当作透明区域。

绘制使用 GDI：

1. `CreatePen()` 创建画框线条。
2. `Rectangle()` 绘制矩形。
3. `FillRect()` 绘制标签背景。
4. `DrawTextW()` 绘制文字。

## 11. ctypes 调用系统 API

`ctypes` 是 Python 调用 C 接口和系统 DLL 的标准库。

项目中用它调用：

1. `user32.dll`：窗口枚举、窗口创建、消息循环、窗口显示隐藏。
2. `gdi32.dll`：画笔、矩形、文字绘制。
3. `kernel32.dll`：进程句柄、模块信息。
4. `psapi.dll`：读取窗口所属进程名。

使用 `ctypes` 的好处是不需要额外安装 pywin32，也能直接访问 Windows 原生能力。缺点是代码更底层，需要手动声明参数类型和结构体。

## 12. JSON 配置和深度合并

项目配置全部使用 JSON。

JSON 的优点是直观、跨语言、容易手动编辑。项目通过 `deep_merge()` 保证默认配置和用户配置可以组合使用。

这套配置方式有两个重要规则：

1. 新增配置项时，只要写进 `DEFAULT_CONFIG`，旧配置文件也能继续运行。
2. 用户配置只覆盖自己写过的字段，不会破坏未写字段的默认值。

这种方式很适合脚本工具，因为它兼顾了易用性和向后兼容。

## 13. 文本匹配和大小写处理

项目使用简单直接的字符串匹配。

支持两种模式：

1. `exact`：完全相等才算命中。
2. `contains`：OCR 文本包含目标文本就算命中。

大小写处理使用 `casefold()`。它比普通 `lower()` 更适合做不区分大小写的比较，尤其是包含非英语文本时更稳。

目前项目没有使用模糊匹配或编辑距离算法，所以匹配逻辑更可控，但对 OCR 错字没有自动纠错能力。

## 14. 坐标缩放和区域映射

项目里有三套坐标需要区分：

1. OCR 图片坐标：OCR 识别结果所在的原始图片坐标。
2. OBS 浏览器源坐标：Canvas 实际显示尺寸。
3. Windows 屏幕坐标：桌面透明层实际覆盖的位置。

OBS 浏览器源通过比例缩放解决：

```text
显示 x = OCR x * canvas.width / OCR 图片 width
显示 y = OCR y * canvas.height / OCR 图片 height
```

桌面透明层也使用类似思路，但它还要先确定透明窗口覆盖的真实屏幕区域。`screen_region` 模式就是为了解决 OBS 源截图坐标和 Windows 屏幕坐标不一致的问题。

这个坐标映射是项目能正确画框的关键。

## 15. 日志、容错和退出控制

项目在可靠性上做了多处处理：

1. `worker.log` 记录运行状态和异常。
2. OCR 初始化失败时不会直接退出，而是继续运行并推送空框。
3. OBS WebSocket 请求失败时会关闭连接，下次重新连接。
4. 主识别循环捕获异常，避免单轮失败导致整个 worker 崩溃。
5. 浏览器 WebSocket 断开后会自动重连。
6. 程序收到退出信号后会清理 server、OBS WebSocket 和桌面透明层。
7. 如果正常退出卡住，会用延迟强制退出计时器兜底。

这种容错策略适合长期运行的直播辅助脚本：局部错误尽量不影响整体进程，配置错误或外部连接问题通过日志排查。

## 16. RapidOCR 原生 TensorRT 实测踩坑

本节记录 2026-08-05 在 Python 3.12、CUDA 12.9、TensorRT 10.16、RTX 2070 上实际接入时遇到的问题，避免以后重复排查。

### 1. 原生 TensorRT 不等于 ONNX Runtime TensorRT EP

最容易混淆的是两者名字相同：

- ONNX Runtime TensorRT EP 是 `TensorrtExecutionProvider`。
- 本项目要求的是 RapidOCR 自己的 `TRTInferSession`。

实现时不能只给 ONNX Runtime provider 列表增加 TensorRT，也不能把 `engine_type` 写成普通字符串。worker 必须把 `EngineType.TENSORRT` 枚举传给 Det、Rec 和可选 Cls。

### 2. RapidOCR 3.8.4 的 `model_root_dir` 初始化缺陷

实测首次进入 TensorRT 时出现：

```text
AttributeError: 'TRTInferSession' object has no attribute 'model_root_dir'
```

原因是 RapidOCR 3.8.4 的 `TRTInferSession.__init__()` 调用 `_get_engine_path()` 前，没有先给实例创建 `model_root_dir`；而 `_get_engine_path()` 会直接读取该属性。

约束是不修改 `venv`、`site-packages` 或 RapidOCR 源文件，所以 worker 使用版本限定的进程内兼容处理：

1. 只在 RapidOCR 版本严格等于 `3.8.4` 时执行。
2. 导入原生 `TRTInferSession`。
3. 如果类本身没有 `model_root_dir`，设置一个类级默认值 `None`。
4. RapidOCR 随后按它原有逻辑从 cfg 延迟设置实例属性。

这个处理没有替换 TensorRT 实现，也没有覆盖 model path、cache dir、workspace 或动态 shape。升级 RapidOCR 后不会继续强行应用。

### 3. `Building TensorRT engine` 看似卡住

日志停在下面一行后，TensorRT 会同步执行 tactic 搜索和 Engine 序列化，期间可能长时间没有进度输出：

```text
Building TensorRT engine (this may take a few minutes)...
```

Engine 只会在 `build_serialized_network()` 完整返回后写入磁盘。构建过程中手动终止 worker，不会留下可恢复的半成品；下一次启动仍会从当前组件开头重建。因此连续多次在 Det 构建完成前终止，就会误以为“每次启动都卡在同一个地方”。

正确做法是让第一次 Det、Rec 构建完整结束。可以在另一个终端观察进程和缓存：

```powershell
Get-Process python | Select-Object Id,CPU,WorkingSet,StartTime
nvidia-smi -l 2
Get-ChildItem .\venv\Lib\site-packages\rapidocr\models\models\*.engine
```

构建成功后会看到 `TensorRT engine saved to ...` 和 `TensorRT engine loaded: ...`。后续相同模型、架构和精度会直接加载缓存。

### 4. FP32 与 FP16 必须分别构建

RapidOCR 缓存文件名包含精度：

```text
*_sm75_fp32.engine
*_sm75_fp16.engine
```

因此 FP32 已成功并不代表 FP16 已准备好，反向也一样。第一次切换精度再次出现 Building 是预期行为，不是缓存失效。实测已成功生成 Det/Rec 四个文件：FP32 两个、FP16 两个。

### 5. `use_cls=false` 仍可能看到 ONNX Runtime Cls 日志

TensorRT 参数只把实际使用的 Det、Rec 切换到 TensorRT。`use_cls=false` 时不为 Cls 构建 TensorRT Engine，分类器保持 RapidOCR 默认 ONNX Runtime。因此初始化日志中出现一次 `Using engine_name: onnxruntime` 不代表 Det/Rec 回退；应结合最终 worker 日志确认：

```text
RapidOCR 原生 TensorRT：精度=FP32；实际后端 Det=TensorRT、Rec=TensorRT；Cls 未启用（保持默认 ONNX Runtime）
```

### 6. CUDA 版本和 Python 包名必须对齐

RapidOCR 原生实现直接导入：

```python
import tensorrt as trt
from cuda.bindings import runtime as cudart
```

CUDA 12 环境应安装 `tensorrt-cu12` 和 CUDA 12 系列 `cuda-python`，不能仅凭显卡驱动显示的“最高 CUDA Version”去安装 cu13。驱动能兼容 CUDA 13，不代表当前 Python 运行库和 TensorRT 就是 cu13。

当前实测组合是 TensorRT cu12 `10.16.1.11`、cuda-python `12.9.7`、CUDA Runtime `12.9`。worker 会一次性列出两项导入失败原因，不会自动安装依赖。

### 7. CUDA / DirectML 勾选在 TensorRT 模式下被忽略

`use_cuda` 和 `use_dml` 属于 ONNX Runtime。GUI 选择 FP32/FP16 后会禁用复选框，但不会清空它们；切回 ONNX Runtime 后原状态恢复。这样可以保留用户的 ORT 配置，同时确保 TensorRT 参数字典中不混入 ORT provider 参数。

### 8. 热重载与失败回退的边界

- 修改 `config.json`：worker 会热重载并尝试切换后端。
- 修改 Python 源码：运行中的解释器不会自动加载，必须重启 worker。
- 新后端初始化失败且已有旧引擎：继续使用旧引擎。
- 首次 TensorRT 初始化失败且没有旧引擎：服务继续运行，但 OCR 暂时发送空框。

日志必须区分“用户请求的后端”和“当前继续运行的实际后端”，否则容易把失败回退误认为切换成功。

### 9. Engine 缓存不是可移植构建产物

Engine 和模型、TensorRT 版本、CUDA 环境、GPU 架构及精度有关。RapidOCR 文件名至少区分模型、`sm75` 和 FP32/FP16，但仍不应把 Engine 当成跨机器通用文件。

规则是：

1. 不提交 `.engine` 到 Git。
2. 不把 RTX 2070 生成的缓存直接当作其他 GPU 的发布资产。
3. TensorRT / CUDA / GPU 变化后，如果旧缓存无法反序列化，让 RapidOCR 重建。
4. 不为了“统一路径”自行覆盖 RapidOCR 默认 cache dir，除非有明确、可复现的必要性。

---

# 三、GUI 控制台技术分析

## 1. GUI 整体定位

`gui.py` 是 v4 新增的本地桌面控制台，基于 PySide6（Qt for Python）实现。

它的定位是 worker 的可视化控制入口，本身不做 OCR，也不直接参与识别循环。它解决的是“以前要手动改 `config.json`、手动改 `name.txt`、手动开命令行启动 worker、手动翻日志”这一串繁琐操作。

它和已有控制入口的关系是：

| 控制入口 | 运行环境 | 适用场景 |
| --- | --- | --- |
| `obs_name_ocr.py` | OBS 脚本面板内 | 已经在 OBS 里操作，想顺手启停 worker |
| `gui.py` | 独立桌面程序 | 想集中编辑配置、目标文字、测试 OBS、看日志 |

两者都不做 OCR，都是把重任务交给独立的 worker 进程。区别在于 `gui.py` 把配置编辑、目标管理、OBS 连接测试、日志查看整合到了一个界面里。

`gui.py` 的核心功能可以概括为五块：

1. 编辑并保存 `name.txt` 目标文字。
2. 以表单方式编辑并保存 `config.json`。
3. 启动和停止 worker 进程。
4. 测试 OBS WebSocket 连接，并可视化选择捕获对象。
5. 定时刷新并显示 worker 最近日志。

## 2. 界面结构和布局

主窗口是 `MainWindow`，继承自 `QMainWindow`，初始大小 1180 x 820。

整体布局自上而下分三层：

```text
顶部工具栏 toolbar
  -> 标题 + 启动 worker + 停止 worker + 运行状态
中部主体 QSplitter（左右可拖动）
  -> 左：目标文字面板（name.txt 编辑）
  -> 右：配置管理面板（config.json 表单）
底部日志面板
  -> 最近 5 行 worker 日志
```

中部使用 `QSplitter` 做左右分栏，左右比例默认 4:6，并且禁止把任一侧拖到完全折叠（`setChildrenCollapsible(False)`）。

右侧配置面板内容较多，放在 `QScrollArea` 里，超出高度时可以滚动。配置项按功能分成几个 `QGroupBox`：

| 分组 | 对应配置 |
| --- | --- |
| 基础服务 | `interval_ms`、`host`、`port` |
| 截图来源 | `capture.*` 和 `capture.obs_websocket.*` |
| 匹配规则 | `match.*` |
| OCR 设置 | `ocr.backend`、`ocr.use_cuda`、`ocr.use_dml` 等 `ocr.*` 字段 |
| 叠加层设置 | `overlay.*` 和 `desktop_overlay.*` |

每个分组用 `QFormLayout` 做“标签 + 控件”的对齐排列，界面由一组 `_build_*` 和 `_add_*_group` 方法分别构建，职责清晰。

## 3. 配置表单双向绑定

配置面板的核心是 `config.json` 和界面控件之间的双向绑定。

`gui.py` 内置了一份和 worker 一致的 `DEFAULT_CONFIG`，并复用了同样的 `deep_merge()` 深度合并逻辑，保证读到的配置始终结构完整。

读取方向（配置 -> 界面）：

1. `load_config_file()` 用 `utf-8-sig` 编码读取 `config.json`（兼容带 BOM 的文件）。
2. 用 `deep_merge(DEFAULT_CONFIG, data)` 补齐缺失字段。
3. `populate_config_form()` 把各字段写入对应控件。

写入方向（界面 -> 配置）：

1. `build_config_from_form()` 先做合法性校验，例如 `interval_ms >= 100`、`port` 在 1-65535、`min_confidence` 在 0-1。
2. 校验通过后把控件值写回配置字典。
3. `save_config_file()` 以 2 空格缩进写回 `config.json`。

这里有几个辅助函数让绑定更稳：

| 函数 | 作用 |
| --- | --- |
| `ensure_dict()` | 确保嵌套字段是字典，避免写入时报错。 |
| `set_combo_data()` | 按数据值而不是显示文本选中下拉项。 |
| `bool_value()` | 把字符串、布尔等多种写法统一成布尔。 |
| `normalize_ocr_backend()` | 将缺失或非法的 OCR 后端值安全归一化为 `onnxruntime`。 |
| `parse_screen_region()` | 把 `screen_region` 文本解析成 `auto` 或矩形字典。 |
| `format_screen_region()` | 把 `screen_region` 配置值还原成可编辑文本。 |

OCR 后端没有用两个可同时勾选的复选框，而是一个互斥的 `QComboBox`：

| GUI 显示 | 保存值 |
| --- | --- |
| 关闭 TensorRT（ONNX Runtime） | `onnxruntime` |
| TensorRT FP32（准确度优先） | `tensorrt_fp32` |
| TensorRT FP16（速度优先） | `tensorrt_fp16` |

`populate_config_form()` 先用 `normalize_ocr_backend()` 归一化值，因此旧配置缺少 `backend` 或配置值非法时，界面都会显示 ONNX Runtime。下拉框的 `currentIndexChanged` 信号会立即调用 `update_ocr_backend_controls()`：

- ONNX Runtime 模式启用 CUDA、DirectML 复选框。
- TensorRT FP32/FP16 模式禁用这两个只属于 ONNX Runtime 的复选框。
- 禁用不会清空原有勾选状态；切回 ONNX Runtime 后会恢复原状态。

`build_config_from_form()` 从 `copy.deepcopy(self.config)` 开始，只覆盖界面负责的字段，所以 GUI 读取并保存配置时不会删除未知的顶层或嵌套字段。

保存配置后通常不需要重启 worker：worker 会按 `reload_files_interval_ms` 热重载 `config.json`，后端或精度变化会在下一次重载时触发 OCR 引擎重建/缓存加载。这也是 GUI 和 worker 解耦的关键——GUI 只管写文件，worker 只管读文件。修改 `worker.py`、`gui.py` 等 Python 源码则必须重启对应进程。

## 4. name.txt 文本编辑

左侧面板用一个 `QTextEdit` 直接编辑 `name.txt`，每行一个目标文字。

规则和 worker 的读取规则保持一致：空行会被忽略，`#` 开头的行用于设置后续目标的分组。

1. `load_name_file()` 读取并显示文件内容。
2. `save_name_file()` 用 UTF-8 写回。
3. 编辑器关闭富文本（`setAcceptRichText(False)`），避免粘贴时混入格式。

和配置一样，保存后无需重启 worker，等待下一次热重载即可。

## 5. worker 进程管理

`gui.py` 用 `subprocess` 把 worker 作为独立子进程来管理，而不是在 GUI 进程里跑 OCR。

启动逻辑 `start_worker()`：

1. 先判断 worker 是否已经在运行，避免重复启动。
2. 检查 `worker.py` 是否存在。
3. 用 `choose_python_executable()` 选择解释器：优先用 `venv/Scripts/python.exe`，找不到才退回当前 `sys.executable`。
4. 用 `subprocess.Popen()` 启动 worker，工作目录设为项目根目录。
5. worker 的标准输出和错误重定向到 `gui_worker.log`。
6. Windows 下加上 `CREATE_NEW_PROCESS_GROUP`，让子进程独立成组。

停止逻辑 `stop_worker()` 采用“先礼后兵”：

1. 先调用 `terminate()` 请求 worker 正常退出。
2. 用 `QTimer.singleShot(3000, ...)` 设定 3 秒后检查。
3. 如果 worker 还没退出，再调用 `kill()` 强制结束。

运行状态由 `worker_timer` 每秒触发的 `update_worker_state()` 维护：通过 `poll()` 判断进程是否存活，更新状态标签的文字和颜色（运行中为绿色，未运行为灰色），并联动启动、停止按钮的可用状态。如果检测到 worker 已退出，会读取退出码并提示。

关闭窗口时 `closeEvent()` 会判断 worker 是否还在运行，弹出“是 / 否 / 取消”询问，避免误关 GUI 导致 worker 被遗留或被意外终止。

## 6. OBS 连接测试与捕获对象选择

这一块让用户不用手动跑 `list_obs_sources.py` 也能配置 OBS 来源。

注意 `gui.py` 自己实现了一套 OBS WebSocket 客户端逻辑，和 worker 里的实现相互独立：

1. `connect_obs()`：建立连接，处理 Hello / Identify 握手。
2. `build_obs_auth()`：当 OBS 开启密码时，用 salt + challenge 做 SHA256 认证。
3. `obs_request()`：发送请求（op 6），按 `requestId` 匹配响应（op 7）。
4. `run_obs_test()`：请求 `GetVersion`，用于“测试连接”。
5. `run_obs_sources()`：请求 `GetSceneList` 和 `GetInputList`，用于“获取捕获对象”。

获取到场景和输入源后，`SourcePickerDialog` 用一个 `QTableWidget` 列出“类型 / 名称 / 子类型 / UUID”，双击或点确定即可选择。选中后会同时回填 `source_name` 和 `source_uuid`，并自动把来源切换为 `obs_websocket`。

`obs_error_message()` 还把常见报错翻译成更易懂的中文提示，例如超时、连接被拒、认证失败。

为了不卡界面，所有 OBS 网络操作都放在 `OBSTaskThread` 后台线程里执行（见第 8 节）。

## 7. 日志查看机制

底部日志面板显示 worker 的最近 5 行日志。

1. `log_timer` 每秒触发 `refresh_log()`。
2. `refresh_log()` 调用 `read_tail_lines(WORKER_LOG_PATH, 5)`。
3. `read_tail_lines()` 不会整文件读取，而是 `seek` 到文件末尾往前读最多 64KB，再取最后几行。

这种“只读文件尾部”的做法很重要，因为 `worker.log` 可能很大（项目里已经出现过上 MB 的日志）。整文件读取会卡顿，而读尾部几乎不受文件大小影响。

这里要区分两个日志文件：

| 文件 | 内容 | 谁在写 |
| --- | --- | --- |
| `worker.log` | worker 自己的运行日志（启动、性能、异常） | worker |
| `gui_worker.log` | GUI 启动 worker 时捕获的标准输出 / 错误 | GUI |

日志面板显示的是 `worker.log`。`QPlainTextEdit` 设了 `setMaximumBlockCount`，只保留少量行，避免长时间运行后界面文本无限增长。

## 8. 线程模型和界面响应性

GUI 程序最忌讳在主线程里做耗时操作，否则界面会卡死。`gui.py` 的线程模型是：

1. 主线程跑 Qt 事件循环，负责界面渲染和交互。
2. OBS 连接、获取来源这类阻塞网络操作放进 `OBSTaskThread`（`QThread`）后台执行。
3. 后台线程内用 `asyncio.run()` 跑异步的 OBS 逻辑，完成后通过信号把结果发回主线程。
4. worker 进程状态和日志刷新用 `QTimer` 轮询，而不是死循环。

`OBSTaskThread` 用信号和主线程通信：

| 信号 | 含义 |
| --- | --- |
| `succeeded(action, result)` | 操作成功，附带结果数据 |
| `failed(message)` | 操作失败，附带错误提示 |
| `finished` | 线程结束（Qt 内置信号） |

操作期间相关按钮会被禁用（`update_obs_controls()` 里的 `busy` 判断），防止重复点击。这套“后台线程 + 信号槽”的模式是 Qt 里处理耗时任务的标准做法。

## 9. 样式和视觉设计

界面在 `main()` 里设置了 `Fusion` 风格，再叠加一份 QSS 样式表（`_apply_style()`）做统一外观。

样式通过 `objectName` 选择器区分不同元素：

| objectName | 用途 |
| --- | --- |
| `toolbar` / `panel` / `logPanel` | 卡片式容器 |
| `appTitle` / `sectionTitle` | 标题文字 |
| `primaryButton` / `successButton` / `dangerButton` / `neutralButton` | 不同语义的按钮配色 |
| `statusBadge` | 圆角状态徽标 |
| `hint` | 浅色提示文字 |

整体走浅色卡片风格：白色卡片、圆角、浅灰描边、蓝色聚焦边框。按钮按语义配色——启动是绿色、停止是红色、主要操作是蓝色、辅助操作是深灰。运行状态徽标还会随 worker 状态切换底色（运行中绿色，未运行灰色），让状态一眼可辨。

## 10. GUI 涉及的关键技术点

| 技术点 | 在 GUI 中的作用 |
| --- | --- |
| PySide6 / Qt Widgets | 提供窗口、布局、控件等界面基础设施。 |
| 信号与槽（Signal / Slot） | 控件事件和后台线程结果的通信机制。 |
| QThread | 把阻塞的 OBS 网络操作移出主线程，避免界面卡死。 |
| QTimer | 定时轮询 worker 状态和刷新日志。 |
| subprocess | 以独立进程方式启动 / 停止 worker。 |
| QSS 样式表 | 用类 CSS 语法统一界面外观。 |
| asyncio | 在后台线程里运行异步 OBS WebSocket 逻辑。 |
| JSON + deep_merge | 复用 worker 的配置合并规则，保证结构完整。 |

和 worker 不同，GUI 的技术重心在“界面响应性”和“进程 / 文件协作”上：它通过读写 `config.json`、`name.txt` 与 worker 协作，通过 `subprocess` 管理 worker 生命周期，通过后台线程保证界面不卡顿。这种“控制台 + 独立 worker”的分工，让配置管理和实际识别彻底解耦。
